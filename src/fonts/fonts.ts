import { Inter, Poppins, Limelight } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const Lime_light = Limelight({
  weight: ["400"],
  subsets: ["latin"],
});

export const poppin = Poppins({
  weight: "400",
  subsets: ["latin"],
});
